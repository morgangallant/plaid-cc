sources = ['client.cc', 'plaid.pb.cc', 'status.cc', 'transport.cc']
plaidlib = shared_library('plaidcc',
                          sources,
                          include_directories : inc,
                          dependencies : deps,
                          install : true)